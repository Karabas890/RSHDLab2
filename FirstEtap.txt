ssh -J s368527@helios.cs.ifmo.ru:2222 postgres2@pg121
# Установка переменных окружения
PGDATA=$HOME/hpd54
PGLOCALE=ru_RU.KOI8-R
PGENCODE=KOI8-R
PGUSERNAME=postgres2
export PGDATABASE=postgres

export PGDATA PGLOCALE PGENCODE PGUSERNAME

# Создание каталога под кластер
mkdir -p $PGDATA

# Инициализация кластера
initdb --locale=$PGLOCALE --encoding=$PGENCODE --username=$PGUSERNAME

# Запуск сервера
pg_ctl -D "$PGDATA" -l "$PGDATA/logfile" start
